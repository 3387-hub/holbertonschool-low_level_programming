#include "holberton.h"
/**
 *
 *
 */

void times_table(void)
{
	int e, i;

	for (e = 1; e <= 9; e++)
	{
		for (i = 1; i <= 9; i++)
		{
			if (e + i >= 1)
			_putchar(e *i );

			else
				_putchar(0);
		}
		_putchar(44);
		_putchar(10);
	}

}
