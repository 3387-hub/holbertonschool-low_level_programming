#include "holberton.h"
/**
 *mul - This function multiplies two integers
 *@a: this variable is an aleatory number
 *@b: this variable is an another aleatory number
 */

int mul(int a, int b)
{
	int result = a*b;
		return (result);
}
